{
  "note": "task-2027 unwired-module ratchet floor. Modules chart-engine/src/index.ts exports that apps/web/src never imports (production code only, deep-import convention — see check_unwired_modules.mjs). Growth past this list fails; a module getting wired is reported as improvable but never auto-removed here — a human commits the smaller list once its wiring task lands. core/renderer, core/series, core/klinechartsBackend, core/klinecharts, core/klinechartsSeries: apps/web talks to the vendor klinecharts fork directly instead of chart-engine's abstract renderer/series backend (see ChartToolbar.tsx/ChartPage.tsx header comments) — spec CH-14 (§9.11) predecessor surface. render/plotRenderers, render/scaleBinding, render/fillBetween: CH-15/CH-15b (task-2002, in_progress). legend/statusLine, legend/dataWindow: CH-16 (task-2013 covers the sibling objectTree persistence gap; statusLine/dataWindow are the untouched half — see ChartLegend.tsx/ChartPanes.tsx header comments). compute/workerPool wired by task-2039 (apps/web's indicatorComputePool.ts now imports createBrowserWorkerPoolBackend/createWorkerPool and IndicatorParityPanel.tsx dispatches computeIndicatorSeries through the resulting pool) — removed from this list. compute/verifiedIndicators wired by task-2038 (IndicatorParityPanel.tsx's buildRow now imports isVerifiedIndicator and checks it before computeIndicatorSeries) — removed from this list. drawings/serialize: ChartPage.tsx wires drawings/tools+drawings/model but never serializeDrawings/deserializeDrawings — CH-4b (task-2012, assigned) needs this to talk to the drawings persistence API. render/lod, render/viewport wired by task-2043 (apps/web/src/routes/chart/useVisibleCandles.ts:14-15 imports downsampleLOD/cullToViewport; ChartPanes.tsx:190 calls useWiredCandleRenderer, which feeds the culled+downsampled series into the mounted CandlestickChart's `data` prop via useVisibleCandles.ts's wireCandleRenderer) — removed from this list.",
  "generated_at": "2026-09-07T01:39:53+00:00",
  "modules": [
    "core/klinechartsBackend",
    "core/klinecharts",
    "core/klinechartsSeries",
    "core/renderer",
    "core/series",
    "drawings/serialize",
    "legend/dataWindow",
    "legend/statusLine",
    "render/fillBetween",
    "render/plotRenderers",
    "render/scaleBinding"
  ]
}
