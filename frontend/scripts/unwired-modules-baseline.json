{
  "note": "task-2027 unwired-module ratchet floor. Modules chart-engine/src/index.ts exports that apps/web/src never imports (production code only, deep-import convention — see check_unwired_modules.mjs). Growth past this list fails; a module getting wired is reported as improvable but never auto-removed here — a human commits the smaller list once its wiring task lands. core/renderer, core/series, core/klinechartsBackend, core/klinecharts, core/klinechartsSeries: apps/web talks to the vendor klinecharts fork directly instead of chart-engine's abstract renderer/series backend (see ChartToolbar.tsx/ChartPage.tsx header comments) — spec CH-14 (§9.11) predecessor surface; task-2053 re-verified these 5 stay unwired (core/renderer, core/series are vendor-free but have no CH-14 pane adapter consumer yet; core/klinecharts/klinechartsBackend/klinechartsSeries are vendor-delegation adapters with no production consumer — splitting them would be a speculative vendor reimplementation, ADR-2026-09-06-F D3). render/fillBetween: only named in ChartPlotLayer.tsx's header comment (task-1732), never actually deep-imported — CH-15b (task-2002) still owns wiring it. compute/workerPool wired by task-2039 (apps/web's indicatorComputePool.ts now imports createBrowserWorkerPoolBackend/createWorkerPool and IndicatorParityPanel.tsx dispatches computeIndicatorSeries through the resulting pool) — removed from this list. compute/verifiedIndicators wired by task-2038 (IndicatorParityPanel.tsx's buildRow now imports isVerifiedIndicator and checks it before computeIndicatorSeries) — removed from this list. drawings/serialize: apps/web only reaches toDrawingsDocument from useChartDrawings.test.ts (a test file, doesn't count) — ChartPage.tsx wires drawings/tools+drawings/model but no production file calls serializeDrawings/deserializeDrawings yet — CH-4b (task-2012, assigned) needs this to talk to the drawings persistence API. render/lod, render/viewport wired by task-2043 (apps/web/src/routes/chart/useVisibleCandles.ts:14-15 imports downsampleLOD/cullToViewport; ChartPanes.tsx:190 calls useWiredCandleRenderer, which feeds the culled+downsampled series into the mounted CandlestickChart's `data` prop via useVisibleCandles.ts's wireCandleRenderer) — removed from this list. legend/dataWindow wired by task-2044 (root cause: any file typing anything out of core/klinecharts.ts — even type-only — drags the whole vendor/klinecharts tree into whatever tsc program reaches it, since that vendor source isn't authored against apps/web's verbatimModuleSyntax/erasableSyntaxOnly flags; fix was a type-boundary split, not a value-vs-type-only fix — see legend/dataWindow.ts/legend/dataWindowStyle.ts docstrings. apps/web/src/routes/chart/DataWindowPanel.tsx:13 imports computeDataWindowRows from @aios/chart-engine/src/legend/dataWindow; ChartPanes.tsx:297 mounts DataWindowPanel fed by mainOverlays+subOverlays+overlaySeries+candles+crosshairTimeMs) — removed from this list. legend/statusLine wired by task-2045 (root cause reproduced: statusLine.ts typed CandleStyle/CandleTooltipStyle/CrosshairStyle/DeepPartial/KLineData/NeighborData/TooltipLegend out of core/klinecharts.ts, same vendor-drag failure as legend/dataWindow — fix reused task-2044s type-boundary split rather than inventing a second one: buildStatusLineLegends stayed vendor-free in statusLine.ts, the vendor-typed createStatusLineTooltipStyle/createCrosshairValueStyle moved to statusLineStyle.ts. apps/web/src/routes/chart/StatusLine.tsx imports buildStatusLineLegends from @aios/chart-engine/src/legend/statusLine and reuses DataWindowPanel.tsx exported resolveDataIndex; ChartPanes.tsx:266 mounts <StatusLine candles crosshairTimeMs> between the pane grid and ChartLegend) — removed from this list. render/plotRenderers, render/scaleBinding wired by task-2078 (ChartPlotLayer.tsx:33-44 deep-imports decodePlotSpec/deriveOverlayPlotSpec/renderPlot/PlotRenderTarget etc. from render/plotRenderers and bindScale/ScaleBindingError from render/scaleBinding; check:unwired-modules reported both as INFO improvable) — removed from this list.",
  "generated_at": "2026-09-07T06:19:46+00:00",
  "modules": [
    "core/klinechartsBackend",
    "core/klinecharts",
    "core/klinechartsSeries",
    "core/renderer",
    "core/series",
    "drawings/serialize",
    "render/fillBetween"
  ]
}
