{
  "note": "task-2027 unwired-module ratchet floor. Modules chart-engine/src/index.ts exports that apps/web/src never imports (production code only, deep-import convention — see check_unwired_modules.mjs). Growth past this list fails; a module getting wired is reported as improvable but never auto-removed here — a human commits the smaller list once its wiring task lands. core/renderer, core/series, core/klinechartsBackend, core/klinecharts, core/klinechartsSeries: apps/web talks to the vendor klinecharts fork directly instead of chart-engine's abstract renderer/series backend (see ChartToolbar.tsx/ChartPage.tsx header comments) — spec CH-14 (§9.11) predecessor surface; task-2053 re-verified these 5 stay unwired (core/renderer, core/series are vendor-free but have no CH-14 pane adapter consumer yet; core/klinecharts/klinechartsBackend/klinechartsSeries are vendor-delegation adapters with no production consumer — splitting them would be a speculative vendor reimplementation, ADR-2026-09-06-F D3). render/fillBetween: only named in ChartPlotLayer.tsx's header comment (task-1732), never actually deep-imported — CH-15b (task-2002) still owns wiring it. compute/workerPool wired by task-2039 (apps/web's indicatorComputePool.ts now imports createBrowserWorkerPoolBackend/createWorkerPool and IndicatorParityPanel.tsx dispatches computeIndicatorSeries through the resulting pool) — removed from this list. compute/verifiedIndicators wired by task-2038 (IndicatorParityPanel.tsx's buildRow now imports isVerifiedIndicator and checks it before computeIndicatorSeries) — removed from this list. drawings/serialize wired by task-2101 (CH-4c: apps/web/src/routes/chart/useChartDrawings.ts now deep-imports serializeDrawings directly and uses it to compare the current drawing set against the last one known to match the server, skipping the drawings PUT that chartToolbarLayoutProps.ts's onSave otherwise chains onto every single \"레이아웃 저장\" click even when nothing was drawn) — removed from this list. render/lod, render/viewport wired by task-2043 (apps/web/src/routes/chart/useVisibleCandles.ts:14-15 imports downsampleLOD/cullToViewport; ChartPanes.tsx:190 calls useWiredCandleRenderer, which feeds the culled+downsampled series into the mounted CandlestickChart's `data` prop via useVisibleCandles.ts's wireCandleRenderer) — removed from this list. legend/dataWindow wired by task-2044 (root cause: any file typing anything out of core/klinecharts.ts — even type-only — drags the whole vendor/klinecharts tree into whatever tsc program reaches it, since that vendor source isn't authored against apps/web's verbatimModuleSyntax/erasableSyntaxOnly flags; fix was a type-boundary split, not a value-vs-type-only fix — see legend/dataWindow.ts/legend/dataWindowStyle.ts docstrings. apps/web/src/routes/chart/DataWindowPanel.tsx:13 imports computeDataWindowRows from @aios/chart-engine/src/legend/dataWindow; ChartPanes.tsx:297 mounts DataWindowPanel fed by mainOverlays+subOverlays+overlaySeries+candles+crosshairTimeMs) — removed from this list. legend/statusLine wired by task-2045 (root cause reproduced: statusLine.ts typed CandleStyle/CandleTooltipStyle/CrosshairStyle/DeepPartial/KLineData/NeighborData/TooltipLegend out of core/klinecharts.ts, same vendor-drag failure as legend/dataWindow — fix reused task-2044s type-boundary split rather than inventing a second one: buildStatusLineLegends stayed vendor-free in statusLine.ts, the vendor-typed createStatusLineTooltipStyle/createCrosshairValueStyle moved to statusLineStyle.ts. apps/web/src/routes/chart/StatusLine.tsx imports buildStatusLineLegends from @aios/chart-engine/src/legend/statusLine and reuses DataWindowPanel.tsx exported resolveDataIndex; ChartPanes.tsx:266 mounts <StatusLine candles crosshairTimeMs> between the pane grid and ChartLegend) — removed from this list. render/plotRenderers, render/scaleBinding wired by task-2078 (ChartPlotLayer.tsx:33-44 deep-imports decodePlotSpec/deriveOverlayPlotSpec/renderPlot/PlotRenderTarget etc. from render/plotRenderers and bindScale/ScaleBindingError from render/scaleBinding; check:unwired-modules reported both as INFO improvable) — removed from this list. render/fillBetween wired by task-2127 (CH-15c: as of task-2127 check:unwired-modules reported 0 unwired modules for every entry here regardless of real wiring state — a latent bug, not real wiring: packages/chart-engine/src/index.ts had been refactored to 'export * from \"./index/*\"' barrels, and parseIndexExports only matched named 'export { A } from' blocks, so its module set was always empty and nothing was ever flagged FAIL; task-2127 left this out of scope since check_unwired_modules.mjs was not in its task file list. task-2034 (QA on task-2027) found and fixed this: check_unwired_modules.mjs now walks the `export * from` re-export graph (collectExportedModules) to resolve module ids through however many barrel hops away the actual `export { A } from` statement lives, so the ratchet is load-bearing again — confirmed it still reports exactly the 5 core/* entries below as unwired, matching this baseline, with 0 false negatives. Actual wiring proof for render/fillBetween instead: apps/web/src/routes/chart/ChartPlotLayer.tsx:53 deep-imports FillBetweenError/FillBetweenErrorCode from render/fillBetween and ChartPlotLayer.tsx:208 catches it alongside PlotRenderError/ScaleBindingError, converting a fill_between PlotSpec whose partner series has a mismatched length/timestamps into one PlotLayerIssue instead of an uncaught throw that blanks the whole plot layer; ChartPlotLayer.test.tsx negative test reproduces FILL_BETWEEN_LENGTH_MISMATCH and fails with an uncaught FillBetweenError when that import/catch is removed, proving the wiring is load-bearing) — removed from this list. DEPTH_CH 감사(task-2729)가 task-2127을 D1로 판정한 잔여 사유(negative 1건뿐(>=3 미달), 실패주입 없음, 수치 성능 단언 없음, 게이트 적색 재현 없음)를 task-3111에서 재확인한 결과, 이미 형제 리프의 DEEPEN으로 해소되어 있었다: ChartPlotLayer.test.tsx의 200회 시드 뮬버리32 퍼저(failure injection, 오손 7종 중 fill_target_missing/fill_length_mismatch/fill_time_mismatch가 fill_between 도메인 검증을 전담)와 numeric ms 예산 성능 단언(50 overlays x 500 points, 2000ms), 그리고 describe(\"gate red reproduction: CH-15c fill_between error containment (task-2127)\") 블록(레거시 no-catch 디스패치가 FillBetweenError를 루프 밖으로 던져 GOOD_LINE까지 날리는 적색 vs 출하된 buildPlotLayer가 issue 1건으로 격리하고 GOOD_LINE은 그대로 렌더하는 녹색을 대조하는 실제 before/after 단언)은 task-3098(commit 40c1ff1f)이 추가했다. fillBetween.ts 자체의 실패주입(computeFillSegments에 length/time mismatch 200회 시드 주입)·수치 성능(1만봉 빈번교차 시리즈 1회 계산 1s 예산)·게이트 적색(교차점 보간 없는 naive 버전과의 대조)·D3 프로퍼티 테스트(300 시드)는 task-3087(commit 7a38c2f8)이 추가했다. 두 커밋 모두 origin/main에 이미 merge되어 있으며, task-3111(worktree frontend-3)에서 재실행해 apps/web ChartPlotLayer.test.tsx 9/9 pass, chart-engine fillBetween.test.ts(render/__tests__) 12/12 pass를 확인했다 — 요구된 4개 축(negative>=3/실패주입/수치성능/게이트적색)이 모두 실제 자동화 단언으로 이미 존재하므로 이 리프는 추가 소스·테스트 변경 없이 근거 문서화만 갱신한다.",
  "generated_at": "2026-09-15T09:00:00+00:00",
  "modules": [
    "core/klinechartsBackend",
    "core/klinecharts",
    "core/klinechartsSeries",
    "core/renderer",
    "core/series"
  ]
}
