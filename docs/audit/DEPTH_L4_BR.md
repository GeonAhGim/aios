# DEPTH 감사 — L4/BR done 리프 D0~D3 판정 (ADR-2026-09-09-C)

- 감사일: 2026-09-10 (task-2722, qa-5)
- 대상: `pm/tasks`의 done implement task 중 제목이 `/L4-|BR-/`에 맞는 리프 전수 (55건, 60건 이하이므로 전수 대조)
- 축 하한: 제목이 `L4-`로 시작하는 리프는 ADR-2026-09-09-C의 안전축(R, L4, LA/LB/LC, FA, CM, EO, DC) 중 L4(실행)에 해당 → **D3 하한**. `BR-`로 시작하는 리프(브로커/거래소 커버리지)는 축 코드 목록에 없음 → **D2 하한**.
- 판정 기준(ADR-2026-09-09-C): D0=스텁, D1=happy path+단위 테스트, D2=경계·실패·동시성·복구 테스트 **전부** 충족(negative≥3, 실패 주입 1, 성능 단언 1, 게이트 적색 재현 1), D3=D2 + 적대적/리플레이/다중 인스턴스 증명.
- 방법: 병렬 감사 에이전트 6개(worktree 내 read-only)가 각 리프의 커밋(`git show <commit> --stat`)과 테스트 파일을 직접 열어 negative 테스트 개수·실패 주입·성능 단언·게이트 적색 재현 여부를 실측했다. 코드 수정 없음.

## 요약

- 전체 55건 중 **55건 전부 자기 축의 D 하한 미달**(meets_floor=false 55/55).
- 등급 분포: D0 3건 · D1 51건 · D2 1건 · D3 0건.
- D2 하한(BR-) 대상 24건 중 미달 24건. D3 하한(L4-) 대상 31건 중 미달 31건.
- 가장 흔한 단일 누락 항목: **수치 성능 단언 부재**(거의 전 리프). 다음으로 실패 주입 부재, negative<3, 게이트 적색 재현 부재 순. 즉 기능·부정 케이스 커버리지 자체는 대체로 두텁지만(negative 테스트는 많은 리프에서 3건 이상 확보), ADR이 요구하는 4요소 동시 충족(특히 성능 단언과 실패 주입의 조합)이 조직적으로 빠져 있다.
- 완전 미시작/유령 done 3건 발견(코드·테스트 증거 없음, D0): task-1758(문서만, 테스트 무변경), task-1931(BR-14, 계좌 미확보로 착수 자체가 안 됨), task-2096(해당 id의 커밋이 저장소에 존재하지 않음 — 실제 작업은 task-2021이 수행).
- task-2328/2329는 자기 커밋 필드가 비어 있고 실제 작업이 각각 task-2322/2323(중복 발행)에서 수행됨 — task 발행 중복 자체가 별도 위생 문제.

## 전수 판정표

| id | title | commit | axis | grade | floor met | evidence | missing (D2/D3 미달 근거) |
|---|---|---|---|---|---|---|---|
| 101 | L4 OMS 순수 도메인 리프 L4-01~09 | b4c4f67 | D2 | D1 | **NO** | test_algo_slicer.py: 9 tests, only 1 negative (test_invalid_time_window_raises); no failure-injection, no perf assertion, no gate-red test found | Needs negative tests >=3 (only 1 present), a failure-injection test (simulated crash/DB/network failure), a numeric performance/latency assertion, and a gate/CI red-line regression test |
| 111 | L4 OMS 리프 L4-07~L4-09 (contracts/v1, ports, errors→어댑터 경계) | 5222d01 | D2 | D1 | **NO** | test_repository_ports.py: 4 rejection assertions (test_incomplete_implementation_fails_port_check, test_outbox_row_rejects_unknown_command_type ValidationError, test_mark_retry_and_mark_dead_require_expected_worker 2x TypeError); pure structural Protocol tests, no I/O, no perf assertion, no gate-red test | No failure-injection test (no DB/network simulated failure, protocol-only), no performance/latency numeric assertion, no gate/CI red-line regression test |
| 456 | L4-11 Exchange 내구성 기반 5모듈(error_taxonomy/http_policy/rate_limiter/c... | b638afc | D3 | D1 | **NO** | 5 unit test files, negatives >3 (test_acquire_raises_rate_limited_when_wait_exceeds_timeout, test_skew_exceeding_max_raises_clock_skew_before_signing, test_negative_attempt_rejected); circuit_breaker/rate_limiter simulate repeated call failures; no numeric perf/latency assertion anywhere, no gate-red/regression test, no adversarial/multi-instance test | Missing numeric performance/latency assertion and gate/CI red-line regression test (required for D2); missing adversarial/multi-instance/replay proof (required for D3, floor since title starts with 'L4-11') |
| 1015 | L4-12 Bitget ResilientTransport 통합(_request가 5모듈 경유·서버보정 클럭 서명) | ec4672f | D3 | D1 | **NO** | test_resilient_transport.py: 5 tests, negatives incl. test_5xx_exhausts_retries_and_raises_retryable, test_non_json_body_fails_closed_without_retry, test_circuit_open_blocks_before_any_send; 429/5xx/circuit-open simulate network failures (failure-injection); no perf assertion, no gate-red test, no adversarial/multi-instance proof | Missing numeric performance/latency assertion and gate/CI red-line regression test (required for D2); missing D3 adversarial/multi-instance/replay proof |
| 1519 | L4-13 Adapter ABC 확장(get_open_orders/get_fills/find_order_by_client... | b8b3d6d | D3 | D1 | **NO** | test_adapter_abc_defaults.py + test_factory_guard.py: >10 pytest.raises; failure-injection via test_default_find_order_by_client_id_propagates_api_error; regression guard test_wiring_scanner_detects_non_literal_mode; no numeric performance/latency assertion anywhere | Missing numeric performance/latency assertion (required for D2); missing D3 multi-instance/replay proof beyond a single guard-bypass unit test |
| 1534 | L4-22 paper 체결 모델 4종(fill_model·fee_model·latency_model·venue_profi... | f6746f0 | D3 | D1 | **NO** | test_fill_model.py/test_purity.py: negatives >3 (constructor range checks, empty book, non-positive adv, limit-without-price); failure-injection via poisoned global random tests; no numeric perf/latency assertion, no gate-red regression test, no multi-instance/adversarial proof | Missing numeric performance/latency assertion and gate/CI red-line regression test (required for D2); no multi-instance/replay adversarial proof (required for D3) |
| 1538 | L4-14 OMS outbox 디스패처(application/outbox_dispatcher.py + wiring.py)... | 38e811f | D3 | D1 | **NO** | test_outbox_dispatcher.py + test_concurrent_dispatchers.py + test_stale_worker_late_write.py: strong negatives, genuine 3-worker race proof (real-DB variant), stale-worker crash/lease-expiry failure-injection; no numeric latency/throughput assertion anywhere and no explicit gate/CI red-line regression test | Missing numeric performance/latency assertion (required for D2) and an explicit gate/CI red-line regression test; despite a strong 3-worker race proof, D2's perf-assertion gap blocks the floor |
| 1551 | L4-19 exchanges/common/ws_session.py(하트비트·ack·seq 갭·resync 공용 세션) +... | 1864403 | D3 | D1 | **NO** | test_ws_session.py (~15 tests): negatives incl. ack-failure, malformed-frame x3; failure-injection via OSError connect failures, ConnectionClosed drops, handler exceptions; no numeric perf/latency assertion, no gate-red regression test, no multi-instance/adversarial proof | Missing numeric performance/latency assertion and gate/CI red-line regression test (required for D2); no multi-instance/replay adversarial proof (required for D3) |
| 1553 | L4-15 OMS application/inbox_processor.py(중복 전달 흡수→fills 1행) + order... | 46f350e | D3 | D1 | **NO** | negative=3; 3-worker/1000x duplicate-delivery race proof test_1000_duplicate_deliveries_across_3_workers_produce_exactly_one_fill; no performance assertion; no gate-red test | No numeric performance/throughput assertion and no gate/CI red-line regression test; D2's AND-criteria still unmet |
| 1563 | L4-06 OMS 마이그레이션 <rev1>_oms_order_events_outbox_inbox(orders 컬럼·ver... | 5afa035 | D3 | D1 | **NO** | test_db_transition_trigger.py: real-DB alembic upgrade/downgrade round trip, 5+ CheckViolationError negatives, plus test_worm_sql_rejects_malicious_table_name (SQL-injection-style adversarial test); failure-injection via DB CHECK-constraint errors; no numeric performance/latency assertion, no explicit gate/CI red-line regression test, no multi-worker concurrency proof in this file | Missing numeric performance/latency assertion (required for D2); despite one adversarial injection test, no multi-instance/replay proof to satisfy D3's floor |
| 1564 | L4-07 OMS 어댑터 1/2: core/db/conditional_write.extra_conditions(기본값 없... | 13f805a | D3 | D1 | **NO** | test_order_repository.py: negatives incl. OrderNotFoundError, ConcurrencyConflictError, CheckViolationError (>3); failure-injection via explicit RuntimeError('simulated downstream failure') proving atomic rollback; concurrency race proof test_concurrent_transitions_only_one_succeeds (2 concurrent tx); no numeric performance/latency assertion, no gate/CI red-line regression test | Missing numeric performance/latency assertion (required for D2) and explicit gate/CI red-line regression test; concurrency proof limited to 2 workers, not a full adversarial/multi-instance D3 proof |
| 1565 | L4-08 OMS 어댑터 2/2: adapters/{outbox,inbox,fills,idempotency}_reposi... | 232cd45 | D3 | D1 | **NO** | negative=5 (stale-worker/mark-dead/inbox-wrong-state/idempotency-mismatch rejections); recovery sim test_lease_expired_row_reclaimed_after_recovery_resets_pending; 3-worker race proof test_three_workers_claim_each_row_exactly_once; no numeric performance assertion; no gate/CI red-line test | No numeric latency/throughput performance assertion anywhere, and no gate/CI red-line regression test; D3 floor (D2 plus adversarial/multi-instance proof) not reached despite the strong 3-worker concurrency proof |
| 1566 | L4-09 OMS application/submit_order.py(tx 1개: 멱등 선점→orders INSERT→VA... | 539344e | D3 | D1 | **NO** | negative=5 (gate deny 0 rows, digest mismatch rollback, unknown symbol, fail-closed type error, kill-switch deny 0 rows); concurrency proof test_50_concurrent_same_intent_submits_produce_exactly_one_row; no failure-injection test; no performance assertion; no gate-red test | No failure-injection test (crash/DB-error/dropped-message simulation), no numeric performance/latency assertion, and no gate/CI red-line regression test — required in addition to the existing 50-way concurrency proof to reach the D3 floor |
| 1567 | L4-14b outbox_dispatcher 실DB 통합 테스트 2건(test_concurrent_dispatchers ... | 46ace35 | D3 | D1 | **NO** | negative=5; failure-injection=test_response_loss_under_concurrency_is_still_exactly_once; multi-worker race proof on real DB=test_three_workers_send_each_row_exactly_once_real_db; no performance assertion; no gate-red test | No numeric performance/latency assertion and no gate/CI red-line regression test — strongest candidate but still fails D2's AND-criteria on these two items |
| 1602 | L4-26 OMS application/order_query.py(주문 조회 서비스 함수 + tenant 격리) + te... | 48db481 | D3 | D1 | **NO** | negative=5 (invalid cursor, missing id, cross-tenant leak x2, list excludes other tenant); no failure-injection test; no performance assertion; no gate-red test; no concurrency/multi-instance test | No failure-injection test (simulated DB/network failure), no numeric performance assertion, no gate/CI red-line regression test, and no multi-worker/adversarial-bypass proof |
| 1603 | L4-17 OMS application/{cancel_order,modify_order}.py + order_servic... | 9e7c57c | D3 | D1 | **NO** | negative=8 (cancel/modify rejection matrix); no failure-injection test; no performance assertion; no gate-red test; no concurrency/multi-worker test | No failure-injection test, no performance assertion, no gate/CI red-line test, and no concurrency/multi-instance proof (entirely single-threaded) |
| 1604 | L4-16 OMS application/unknown_resolver.py + order_service/reconcile... | 6e0c368 | D3 | D1 | **NO** | negative=5+ (boundary matrix, fail-closed data integrity); no failure-injection test; no performance assertion; no gate-red test; no concurrency test | No failure-injection test, no performance assertion, no gate/CI red-line test, and no multi-instance/adversarial-bypass proof |
| 1605 | L4-23 paper 시뮬레이터 어댑터 + 마이그레이션 rev3 paper_ledger(exchanges/paper/{l... | 0db9a9b | D3 | D1 | **NO** | negative=4; failure-injection=test_drop_injection_raises_but_ledger_holds_real_terminal_state; adversarial guard-bypass attempt=test_live_configured_subclass_is_blocked_by_defense_in_depth_guard; no performance assertion; no gate-red test | No numeric performance/throughput assertion and no gate/CI red-line regression test present |
| 1758 | L4-31 명세에 행이 없는 구현 파일 소급 등재 + 크기 상한 강제 | 7223a8c | D3 | D0 | **NO** | Commit 7223a8c touches only docs and adds docstrings to 5 existing src files; zero test files added or modified; DoD is only 'existing tests pass unmodified' | entire D2/D3 evidence set is absent: no negative tests, no failure-injection, no performance assertion, no gate/CI red-line test, no adversarial/multi-instance proof — documentation/retroactive-registration task with no new test coverage at all |
| 1776 | L4-32 해외주식을 ExchangeAdapter 정식 계약으로 승격 (사용자 연결로 미국 증시 참여) | 280c9aae | D3 | D1 | **NO** | commit 280c9aae adds 6 parametrized positive USER_SCOPED tests + 1 negative (test_domestic_ticker_is_not_tagged_user_scoped); broader capability/gate coverage lives in task-1786's file | No failure-injection test, no numeric performance/latency assertion, no gate/CI red-line regression-proof test, and no adversarial/multi-instance/replay test — only happy-path plus declarative rejection tests |
| 1779 | BR-1 KIS TR 커버리지 매트릭스 자동 생성 + 래칫 ('100%'를 수치로 만든다) | ff1c3e4 | D2 | D1 | **NO** | negative=5; gate/CI red-line=test_lowered_coverage_fails plus ratchet regression guards; no numeric performance/throughput assertion anywhere | No numeric performance/throughput assertion exists in the suite — only unmet D2 criterion |
| 1780 | BR-2 KIS 호출 한도 프로파일 (실전·모의 상이, TR 그룹별) | b5fbf87 | D2 | D1 | **NO** | only 1 real negative test; has a numeric wait-bound assertion (0.0<sleep<=0.1s) but no failure-injection or gate-red test | needs >=3 rejection/negative tests, a failure-injection test, and a gate/CI red-line regression test |
| 1781 | BR-3 국내주식 조회 전수 완성 (신용잔고·대주, 기간별 매매손익, 재무제표) | 179a8e1 | D2 | D1 | **NO** | 6 negative tests (malformed-response FatalExchangeError); no failure-injection, no perf assertion, no gate-red test | no simulated network/DB failure-injection test, no numeric latency/throughput assertion, no CI gate-red regression test |
| 1782 | BR-4 해외주식 거래소 전수 (현재 US가 NAS/NASD 하나로 뭉쳐 있다) | 88c84cb | D2 | D1 | **NO** | >3 negative tests; no failure-injection, no numeric perf/latency assertion, no gate-red regression test | no failure-injection test, no numeric latency/throughput performance assertion, no CI gate-red regression test |
| 1783 | BR-5 해외주식 실시간 웹소켓 (현재 국내 3종뿐) | 5846531 | D2 | D1 | **NO** | negative/boundary tests present; failure-injection via simulated ConnectionClosed disconnect; no numeric perf/latency assertion, no gate-red regression test | no performance/latency assertion, no gate/CI red-line regression test |
| 1784 | BR-6 국내선물옵션 (전무 — P2에서 P1로 승격) | 863bcc6 | D2 | D1 | **NO** | 4 negative tests; gate-red regression tests re-catch task-1977/2005 REJECT defects; no failure-injection(network/DB/crash), no perf assertion. Safety-relevant (LIVE-guard) but floor stays D2 | no simulated crash/network/DB failure-injection test, no numeric latency/throughput performance assertion |
| 1785 | BR-7 해외선물옵션 (전무) | 20d7190 | D2 | D1 | **NO** | 4 negative tests; gate-red regression tests re-catch task-1978 REJECT path-mismatch defects; no failure-injection, no perf assertion. Safety-relevant (LIVE-guard) but floor stays D2 | no failure-injection test (simulated crash/network/DB), no numeric latency/throughput performance assertion |
| 1786 | BR-8 KIS capabilities 정합 + ExchangeAdapter 계약 승격 (전 자산군) | 7d2b7ef | D2 | D1 | **NO** | 4 negative tests incl gate-bypass call-count checks; no failure-injection, no perf assertion, no test tied to a previously-failing CI gate scenario | no failure-injection test, no numeric latency/throughput performance assertion, no test reproducing a previously-red CI gate scenario |
| 1787 | BR-9 비트겟 커버리지 매트릭스 + 잔여 보완 + 거래소 확장 SPI 정리 | ce02326 | D2 | D1 | **NO** | many negative tests (well over 3); gate-red CI ratchet regression test_lowered_coverage_fails; failure-injection-like missing-file tests; no numeric latency/throughput performance assertion anywhere | no performance assertion (numeric latency/throughput bound) in any BR-9 test file |
| 1788 | BR-10 NH 증권 어댑터를 KIS 기준으로 정렬 (두 번째 증권사) | cf6174f | D2 | D1 | **NO** | 4 negative tests; failure-injection present via simulated httpx.ConnectError network failure; no numeric latency/throughput performance assertion, no gate/CI red-line regression test | no performance assertion (numeric latency/throughput bound), no gate/CI red-line regression test |
| 1924 | P0 CI 적색: pytest 5건 실패 — KIS 해외주식/capabilities (BR-4·BR-8 낙진) | fb1b93e | D2 | D1 | **NO** | negative>=3; commit msg describes a manual red-then-green revert but not an automated mutation/regression test; no automated failure-injection test; no perf/latency assertion | no automated failure-injection test, no numeric performance/throughput assertion; the red-then-green proof was a manual one-off check, not a committed repeatable test |
| 1928 | BR-11 KIS 기준 목록 확장 — 예제 원문에서 path·method·params·응답필드 추출 | 92c3cac | D2 | D1 | **NO** | negative>=3; determinism tests present but no failure-injection, no perf assertion, and no test of the new fail-closed exit-code-1 gate path | no failure-injection test, no performance assertion, and no test exercising the fail-closed (exit 1) gate behavior added by this commit |
| 1929 | BR-12 KIS 어댑터 메서드 자동 생성 (미착수 289건 대상) | a7a919e | D2 | D1 | **NO** | negative>=3; gate-red present via test_render_rest_method_post_gets_paper_sandbox_guard plus drift guards; no failure-injection test; no numeric performance/throughput assertion | no failure-injection test (no simulated crash/DB/network error), no performance/latency/throughput numeric assertion |
| 1930 | BR-13 KIS 계약 테스트 — 계좌 없이 요청 조립·응답 파싱 검증 | 126e271 | D2 | D1 | **NO** | negative>=3 incl mutation-detection tests; gate-red via test_discovers_all_generated_methods (silent case-count regression guard); no numeric performance/latency assertion anywhere | no performance/throughput/latency numeric assertion; failure-injection weakly represented (response-tampering mutation, not infra-level crash/DB/network failure) |
| 1931 | BR-14 KIS 실계좌 왕복 검증 (모의투자 계좌 확보 후) | (none) | D2 | D0 | **NO** | commit field empty; no matching commit in git log for 1931/BR-14/HB-3; tests/integration/exchanges/kis/ directory does not exist; ADR commit a20b6adf states remaining KIS work proceeds without an account (HB-3 never resolved) | No implementation or tests exist anywhere in the repo; task appears never started (blocked on KIS demo account acquisition, bypassed rather than resolved) |
| 1975 | P0 하드가드: BR-12 자동 생성 KIS 주문 메서드 19건에 @require_paper_sandbox 부재(생성기 ... | 063b7e1 | D2 | D1 | **NO** | negative>=3; gate-red/regression-guard clearly present via test_scanner_detects_generator_style_name_via_tr_metadata (reproduces the exact naming-drift bypass); no failure-injection test; no numeric performance assertion | no failure-injection test, no performance/throughput assertion — otherwise close to D2/D3 quality given the adversarial scanner-bypass test |
| 1981 | BR-6 결함 수정(리뷰 task-1977 REJECT): 국내선물옵션 필수 파라미터 2건 누락 + LIVE 거부 beh... | 2d6759c | D2 | D1 | **NO** | only 2 negative tests added (LIVE-adapter rejection x2); no test in this commit verifies ORD_DVSN_CD/RMN_QTY_YN wiring (closed later by task-2008); no failure-injection; no perf assertion; no gate-red test for the parameter-wiring claim itself | negative test count below 3; no test proving the actual parameter fix is wired at this commit; no failure-injection test; no performance assertion |
| 1983 | BR-7 결함 수정(리뷰 task-1978 REJECT): 해외선물옵션 TR-path 오배선 2건 + 모의(V-)/실전 ... | 2978b0f | D2 | D1 | **NO** | negative~3; strong gate-red match (red-then-green regression proof for TR-path mismatch); mutation-detection failure-injection proxy present; no numeric performance/throughput/latency assertion anywhere | no performance/throughput/latency numeric assertion — all other D2 elements arguably present but the strict all-four bar is not met |
| 1990 | [ci:ca36e592d57e] CI 적색: code_language 13463 -> 15200 — BR-12 자동 생성... | 1d6351b | D2 | D1 | **NO** | Only one new test added, a strong literal gate-red match (reproduces the exact code_language CI-red scenario, count 1289->0); AST-equivalence verified per commit message but not committed as an automated test; no negative-input tests (below required 3), no failure-injection test, no numeric performance assertion | negative test count below 3 (only 1 regression test present), no failure-injection test, no performance assertion |
| 2008 | BR-6 결함 수정 2/2(리뷰 task-2005 REJECT): 국내선물옵션 ORD_DVSN_CD·RMN_QTY_YN ... | f816fbbe | D2 | D1 | **NO** | commit f816fbbe adds 2 gate-red regression tests (explicitly verified to fail when field removed) plus pre-existing negatives and LIVE-guard negatives | No failure-injection test (simulated crash/DB/network failure) and no numeric performance assertion anywhere in the futureoption test surface |
| 2096 | BR-13 결함 수정(리뷰 task-2084 REJECT): 계약 테스트가 mypy --strict를 통과하지 않는다 | (none) | D2 | D0 | **NO** | audit_input record has commit=""; no commit for task-2096 exists anywhere in git history; the actual mypy-strict fix was made under task-2021 (commit be6c383e), not task-2096 | No commit/implementation attributable to task-2096 at all — stub/phantom done-record with zero evidence of work |
| 2151 | L4-18a 재시작 복구 1/2: 만료 lease 회수 + 복구 완료 전 submit 거부 | b3645f8a | D3 | D1 | **NO** | commit b3645f8a adds 6 tests covering lease-reclaim boundary, audit-log proof, and gate denial/allow around recovery completion | No numeric performance assertion, no explicit gate/CI red-line regression test, no adversarial/multi-instance/replay test; failure-injection only implicit (seeded dead-process lease) |
| 2179 | L4-30 Bitget 데모 왕복 검증 — place/cancel/get + 잘못된 주문 거부 + BITGET_SPOT_... | 828a4e6b | D3 | D1 | **NO** | structural/negative unit tests present; live-demo roundtrip/tick-violation tests exist but both marked live_demo and explicitly skipped at commit time (missing passphrase) | Core DoD (real Bitget demo roundtrip, tick-violation rejection) never actually executed/passed — BITGET_SPOT_PROFILE.verified still DOC_ONLY, not LIVE_VERIFIED; no failure-injection test, no numeric performance assertion, no multi-instance/adversarial proof |
| 2180 | L4-29 OMS 적대적 2파일(LIVE 모드 우회 시도 3경로 차단 + 변조된 provider 이벤트 거부) | 03efa422 | D3 | D1 | **NO** | commit 03efa422 adds 3-bypass-path test + tampered-provider-event test (venue-spoofing, forged-order-reference, replay-after-terminal-fill) — strong negative, gate-red, and replay-attack coverage | No failure-injection test (simulated DB/network fault) and no numeric performance/latency assertion — D2 baseline not fully satisfied despite genuinely strong adversarial/replay coverage |
| 2183 | L4-20 bitget/private_ws_mixin.py(orders·fill → inbox) + wiring 구독 등록 | 1186dd95 | D3 | D1 | **NO** | task's own commit 1186dd95 is comment-translation only; real implementation commit e32e4ae2 has idempotent duplicate-fill test, 2 fail-closed tests, simulated dropped WS connection (failure-injection), cancellation/leak-cleanup test | No numeric performance assertion, no explicit gate/CI red-line regression test, only 2 true negative/rejection tests (short of >=3), no adversarial/multi-instance/replay test |
| 2310 | L4-18b 재시작 복구 2/2: outbox SENDING 잔여 명령 재진입·UNKNOWN 전환 | 436366d5 | D3 | D2 | **NO** | commit 436366d5 adds explicit crash-between-send-and-commit failure injection, contrasting pre-send crash reentry test, negative for untouched live leases, 2 structural regression-guard tests — negative>=3, real failure-injection, gate-red criteria all satisfied | No numeric performance/latency assertion anywhere, and no multi-worker/concurrent race proof for this specific recovery path — D2 baseline solid but D3's extra adversarial/multi-instance/replay proof is absent |
| 2315 | L4-21 KIS/NH 어댑터 내구성: transport 위임 + 토큰 재발급 단일화(동시 10회 → 1회) + 역조회 | 050aabe0 | D3 | D1 | **NO** | test_kis_durability.py: 3 negatives, failure-injection via scripted 401 responses, gate-red source-check test, asyncio 10-way concurrency test proves single token issuance | No numeric latency/throughput assertion anywhere; concurrency test is single-process asyncio only, not a multi-worker/multi-instance or adversarial/replay proof required for D3 |
| 2316 | L4-24 3자 대사(three_way_reconciler) + 스케줄러 + wiring 등록 — MATERIAL 불일치... | 4f1a05de | D3 | D1 | **NO** | test_three_way_reconciler.py: >=3 negatives, failure-injection via scripted adapter TimeoutError (REC-003), gate-red scenario REC-002 (MATERIAL mismatch denies submit) | No numeric latency/throughput assertion; no multi-instance/adversarial/replay proof (single pool, no concurrent reconcilers or adversarial DENY-bypass attempt) |
| 2322 | L4-27 OMS 관측성 배선: §7.2 메트릭 이름 단일출처 등재 + outbox/inbox/unknown 계측 + §... | 09a985e6 | D3 | D1 | **NO** | negative/reject-format assertions (7 malformed literal names), FakeMetrics wiring proof, redaction negative test masking api_key/raw_payload | No failure-injection test (crash/DB/network) and no numeric latency/throughput assertion; no multi-instance/adversarial/replay proof for D3 |
| 2323 | L4-28 OMS 성능 테스트 4종(§7.1 수치 — 환경 정규화 임계 + 왕복 수 절대 단언) | 034fed00 | D3 | D1 | **NO** | 4 perf files each carry an explicit round-trip-count regression guard (gate-red) asserting round_trips==budget and !=budget+1 | No test asserts a numeric latency/throughput bound (perf gate is DB round-trip count equality, not a performance bound); also no failure-injection test |
| 2328 | L4-27 OMS 관측성 배선: §7.2 메트릭 호출 삽입 + §7.3 로그 필드 + 네이밍 규칙 테스트 | (none) | D3 | D1 | **NO** | own task id has no commit; leaf work found under duplicate task-2322 (commit 09a985e6): naming-rejection tests, wiring-proof tests, redaction masking test | No failure-injection test, no performance assertion (out of scope by design), fewer than 3 distinct rejection/error test functions, no adversarial/multi-instance/replay test |
| 2329 | L4-28 tests/perf/oms 4종 — 제출 내부 경로·outbox·inbox 지연과 처리량 수치 단언 | (none) | D3 | D1 | **NO** | own task id has no commit; equivalent leaf work found under duplicate task-2323 (commit 034fed00): 4 perf files pair printed non-gating latency/throughput with exact DB round-trip-count regression assertions | No failure-injection test anywhere; absolute latency/throughput bounds explicitly non-gating by design, so numeric-bound criterion only weakly satisfied via round-trip-count budgets |
| 2348 | L4-04 거래소 venue_profile 3종 + SymbolRegistry 운영 배선(미등록 심볼 fail-closed) | a809a6c7 | D3 | D1 | **NO** | >=4 negatives, adversarial test_require_verified_rejects_unverified_spec_adversarial, gate-red regression test proving zero prior production instantiations | No failure-injection test (DB error/network failure/crash) and no numeric latency/throughput assertion; no multi-instance/adversarial-bypass proof against the fail-closed gate itself for D3 |
| 2351 | L4-10 executor client_order_id를 idempotency 모듈로 교체 + strategy_execu... | 316cc99d | D3 | D1 | **NO** | test_executor_client_order_id.py: 3 happy-path/idempotency tests only | Zero negative tests, no failure-injection test, no numeric performance assertion, no gate/CI red-line regression test, no multi-instance/concurrent-retry or adversarial proof required for D3 |
| 2514 | BR-15 Bitget 통합계정(UTA, v3 API) 지원 + 계정 모드 감지 — 실측 40085 'Unified Ac... | ec0f4283 | D2 | D1 | **NO** | >=5 negative/pytest.raises tests, gate-red tests proving 40085/40099 now classify as AUTH not UNKNOWN_RESPONSE | No numeric latency/throughput assertion; simulated 40085/40099 are business/auth error codes not infra-level failure-injection; live-account UTA roundtrip explicitly unverified per commit message |

## 발행된 DEEPEN task

D2(BR-)/D3(L4-) 미달 55건 전부에 대해 `orchestrator.create_task`로 `DEEPEN <원 task id> <부족한 증빙>` implement task를 발행했다(우선순위 1, role은 원 task와 동일, tasks/ 직접 쓰기 없음). 발행된 id 목록(원 task id -> DEEPEN task id):


| 원 task id | DEEPEN task id |
|---|---|
| 101 | 2753 |
| 111 | 2754 |
| 456 | 2755 |
| 1015 | 2756 |
| 1519 | 2757 |
| 1534 | 2758 |
| 1538 | 2759 |
| 1551 | 2760 |
| 1553 | 2761 |
| 1563 | 2762 |
| 1564 | 2763 |
| 1565 | 2764 |
| 1566 | 2765 |
| 1567 | 2766 |
| 1602 | 2767 |
| 1603 | 2768 |
| 1604 | 2769 |
| 1605 | 2770 |
| 1758 | 2771 |
| 1776 | 2772 |
| 1779 | 2773 |
| 1780 | 2774 |
| 1781 | 2775 |
| 1782 | 2776 |
| 1783 | 2777 |
| 1784 | 2778 |
| 1785 | 2779 |
| 1786 | 2780 |
| 1787 | 2781 |
| 1788 | 2782 |
| 1924 | 2783 |
| 1928 | 2784 |
| 1929 | 2785 |
| 1930 | 2786 |
| 1931 | 2787 |
| 1975 | 2788 |
| 1981 | 2789 |
| 1983 | 2790 |
| 1990 | 2791 |
| 2008 | 2792 |
| 2096 | 2793 |
| 2151 | 2794 |
| 2179 | 2795 |
| 2180 | 2796 |
| 2183 | 2797 |
| 2310 | 2798 |
| 2315 | 2799 |
| 2316 | 2800 |
| 2322 | 2801 |
| 2323 | 2802 |
| 2328 | 2803 |
| 2329 | 2804 |
| 2348 | 2805 |
| 2351 | 2806 |
| 2514 | 2807 |
