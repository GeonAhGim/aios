<!-- rendered from cloud_dispatch_template.md, task-7620 -->

You are implementing ONE task for the AIOS repo (GeonAhGim/aios). Deliver it as ONE pull request against main, opened READY FOR REVIEW (not draft). Do NOT merge, do NOT push to main.

TASK task-7620: ADR-2026-09-26-B DOM-2 — zone 매니페스트 SCAFFOLD 표기: 5계층 미완 도메인 사유 명시 + 검사기 강제
Spec: ADR-2026-09-26-B Decision 4(DOM-2). src/foundation/<ctx>/의 5계층(domain/application/ports/adapters/contracts) 중 하나 이상이 비어 있는 컨텍스트(실측: marketplace는 ports/adapters 없음)를 .aios-zone에 새 맵 scaffold_reasons: {<ctx>: '<계층>: <사유>'}로 명시하고, scripts/check_zone_manifest.py가 src/foundation/* 하위를 스캔해 5계층 중 없는 것이 있는데 scaffold_reasons에도 없으면 실패(새 코드가 계층 위치를 정하지 않고 방치하는 것을 fail-closed로 막는다)하도록 확장한다. 이 검사는 warn 모드로 도입한다(OPS-42 규약과 같은 정신 — pyproject/CI 배선은 ops 후속 리프, 이 리프는 스캐너 로직과 매니페스트 항목만).
DoD:
1) marketplace(ports/adapters 없음) 등 실제 미완 컨텍스트 전부가 scaffold_reasons에 사유와 함께 등재
2) check_zone_manifest.py가 5계층 결측 + scaffold_reasons 누락 조합을 감지하는 함수 단위 테스트(negative 최소 2건: 결측+사유없음 실패, 결측+사유있음 통과)
3) 현재 실제 .aios-zone/코드베이스 조합이 이 검사를 통과함을 테스트로 확인(자기 회귀)
4) note에 'CI(local_ci.py) 배선은 pm 저장소 파일이라 별도 ops 후속 리프 필요'를 명시
5) 파일당 300줄 상한
Files: .aios-zone, scripts/check_zone_manifest.py, new:tests/unit/scripts/test_zone_manifest_layers.py
Tier:  / Role: claude-local
Hold note: [cloud_session] 클라우드 적합: 스캐너+매니페스트 저장소 완결, 실키/DB 불필요

PR title: <type(scope): ...> (task-7620)
PR body: first line "AIOS task-7620", DoD evidence, test output tail.
