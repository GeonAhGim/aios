<!-- rendered from cloud_dispatch_template.md, task-7600 -->

You are implementing ONE task for the AIOS repo (GeonAhGim/aios). Deliver it as ONE pull request against main, opened READY FOR REVIEW (not draft). Do NOT merge, do NOT push to main.

TASK task-7600: BR-22 Binance 거래소 추가 (b) — 인증/서명·rate-limit 클라이언트 + market_data + capabilities/venue_profile + factory 등록
Spec: docs/exchanges/ADDING_AN_EXCHANGE.md 1~3단계, Binance. auth.py: HMAC-SHA256 서명(signature+timestamp+recvWindow, 순수 함수, 실키 불필요) + 429/418 rate-limit 백오프. market_data_mixin.py: get_ticker/get_orderbook/get_ohlcv(ExchangeAdapter SPI 시그니처 준수). venue_profile.py: BINANCE_SPOT_PROFILE(VenueCapabilityProfile, verified='DOC_ONLY'). factory.py: register_exchange_adapter_factory('binance', ...)로 BR-9 확장점 등록(factory.py if-분기 불변).
DoD:
1) auth.py 서명 함수: 고정 입력 -> 고정 서명값 assert 테스트 최소 3건
2) market_data_mixin이 ExchangeAdapter 추상 시그니처와 일치
3) venue_profile() 메서드 실제 배선 확인(상수만 정의하고 안 쓰는 결함 금지)
4) register_exchange_adapter_factory 호출이 factory.py if-분기를 수정하지 않음
5) 파일당 300줄 상한
Files: new:src/exchanges/binance/auth.py, new:src/exchanges/binance/market_data_mixin.py, new:src/exchanges/binance/venue_profile.py, new:src/exchanges/binance/factory.py
Tier:  / Role: backend
Hold note: [cloud_session] 클라우드 적합: Binance 클라이언트/시세/capability, 저장소만으로 완결

PR title: <type(scope): ...> (task-7600)
PR body: first line "AIOS task-7600", DoD evidence, test output tail.
