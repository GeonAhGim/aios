<!-- rendered from cloud_dispatch_template.md, task-7618 -->

You are implementing ONE task for the AIOS repo (GeonAhGim/aios). Deliver it as ONE pull request against main, opened READY FOR REVIEW (not draft). Do NOT merge, do NOT push to main.

TASK task-7618: ADR-2026-09-26-B SBX-3 — execution_loop가 전략 코드를 같은 프로세스에서 실행하지 않음을 정적 테스트로 고정
Spec: ADR-2026-09-26-B Decision 1(SBX-3). src/services/execution_loop/** (LIVE/PAPER 실주문 루프)가 src.core.script.runtime.interpreter의 execute/compile_source/_Machine을 직접 import하지 않음을 정적으로 고정한다(AST 기반 import 그래프 검사, tests/adversarial/assistant/test_no_execution_access.py의 FORBIDDEN_MODULE_PREFIXES 기법 재사용 — 새 상수만 추가, 기존 파일은 건드리지 않는다). execution_loop는 이미 계산된 전략 평가 결과(신호/주문 의도)만 소비해야 하며, 코드 실행 자체는 SBX-2 sandbox(별도 프로세스)나 FSM 엔진(src/core/strategy, FROZEN_PAPER_ONLY)이 담당한다.
DoD:
1) src/services/execution_loop/ 아래 모든 .py 파일의 import 문을 AST로 파싱해 src.core.script.runtime.interpreter 관련 심볼이 하나도 없음을 assert
2) negative: tmp_path에 위반 import를 넣은 가짜 파일을 만들어 검사 함수가 실제로 위반을 잡는지 확인(최소 1건) — 항상 통과만 하는 허수아비 검사가 아님을 증명
3) 현재 코드베이스가 이미 위반 0건임을 테스트 결과로 확인
4) 파일당 300줄 상한
Files: new:tests/adversarial/execution_loop/test_no_inprocess_script_eval.py
Tier:  / Role: backend
Hold note: [cloud_session] 클라우드 적합: 저장소 완결 정적 import 검사, 실행/DB 불필요

PR title: <type(scope): ...> (task-7618)
PR body: first line "AIOS task-7618", DoD evidence, test output tail.
