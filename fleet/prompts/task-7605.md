<!-- rendered from cloud_dispatch_template.md, task-7605 -->

You are implementing ONE task for the AIOS repo (GeonAhGim/aios). Deliver it as ONE pull request against main, opened READY FOR REVIEW (not draft). Do NOT merge, do NOT push to main.

TASK task-7605: ADR-2026-09-26-B SBX-1 — 전략 인터프리터 런타임 스텝/연산 예산(fail-closed)
Spec: ADR-2026-09-26-B Decision 1(SBX-1). src/core/script/runtime/interpreter.py의 _Machine 실행 루프(_Machine.run의 `for pos, instr in enumerate(ir.instrs)`, 약 158행)에 실행된 명령(instr) 카운터를 두고, 컴파일 시 정적 상한(DSL-6, src/core/script/analysis/resources.py의 ScriptResourceLimitError와는 별개)인 런타임 상한 상수 SCRIPT_RUNTIME_LIMIT(연산 수 기준 정수, 예: 1_000_000)을 초과하면 신규 예외 ScriptRuntimeLimitError를 던져 실행을 fail-closed로 중단한다. DSL-6이 컴파일 시점에 못 잡는 경우(반복 바 수가 런타임 입력 길이에 비례해 커지는 경우)를 실제로 재현하는 테스트가 있어야 한다.
DoD:
1) SCRIPT_RUNTIME_LIMIT 초과 시 ScriptRuntimeLimitError 발생 — 구체 입력: instr 실행 횟수가 SCRIPT_RUNTIME_LIMIT+1이 되는 IR로 재현
2) 정상 스크립트(명령 수 < 상한)는 기존 동작과 동일(회귀 없음 — 기존 tests/unit/core/script/ 스위트 통과)
3) negative(경계값) 최소 3건: 상한-1(통과)/상한(통과 또는 정확히 여기서 차단, 구현에서 결정하고 테스트로 고정)/상한+1(차단)
4) 주석/독스트링에 DSL-6 정적 검사와 다른 경로임을 1줄로 명시(영문)
Files: src/core/script/runtime/interpreter.py, new:tests/unit/core/script/test_runtime_step_budget.py
Tier:  / Role: backend
Hold note: [cloud_session] 클라우드 적합: 순수 인터프리터 로직, 저장소만으로 완결, fake IR로 검증

PR title: <type(scope): ...> (task-7605)
PR body: first line "AIOS task-7605", DoD evidence, test output tail.
