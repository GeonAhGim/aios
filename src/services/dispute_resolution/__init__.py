"""18.2 — Dispute resolution submodules (queries / refund clawback).

Public API stays at `src.services.dispute_resolution_service` (the facade);
import from here only within that package.
"""
from __future__ import annotations
