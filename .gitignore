.venv/
__pycache__/
*.pyc
.env
.pytest_cache/
*.egg-info/
# 루트 로컬 상태(heartbeat)만 — 앵커 없는 runtime/ 는 src/core/script/runtime/(DSL-8)까지 무시했다
/runtime/
.coverage
coverage.xml
# task-2613: hypothesis example database is local-only, never committed.
.hypothesis/
# task-2851 MUT-1: regenerated every scripts/run_mutation.py run -- the ratchet
# state that persists is mutation-score-baseline.json (committed).
/mutation-report.json
# task-3878: OPS-45/46 git hooks (commit-msg, pre-commit) are regenerated per
# worktree by worker_runner.ensure_bom_hooks() (core.hooksPath=.claude/hooks) --
# local-only, never committed. task-7981: session-start.sh is a committed
# Claude Code SessionStart hook (a different "hooks" concept that happens to
# share this directory name), not one of those generated git hooks.
/.claude/hooks/*
!/.claude/hooks/session-start.sh

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# task-5773: root-level node_modules/ leaked as untracked orphan output in
# other worktrees (e.g. root vitest/vite smoke runs) -- frontend/.gitignore
# already covers frontend/node_modules/, this covers the repo root.
/node_modules/
